import styles from "./Header.module.css";
const Header = () => {
  return (
    <div className={styles.header}>
      <b className={styles.aTypicalPage}>A Typical Page</b>
    </div>
  );
};

export default Header;
